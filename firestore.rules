rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /devices/{deviceId} {
      // Only allow authenticated users to read or write device data.
      allow read, write: if request.auth != null;
    }
  }
}
